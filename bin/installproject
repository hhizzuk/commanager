#!/bin/bash

set -Eeuo pipefail
set -x

python3 -m venv env
source env/bin/activate

pip install -r requirements.txt
pip install flask
pip install python-dotenv

cd commanager || { echo "Error: commanager directory not found."; exit 1; }